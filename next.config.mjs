/** @type {import('next').NextConfig} */
const nextConfig = {
  // TypeScript checking now enabled for better type safety
  // Note: Fix any type errors that appear during build
  images: {
    unoptimized: true,
  },
}

export default nextConfig

